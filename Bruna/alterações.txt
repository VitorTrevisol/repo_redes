                    # Conectar ao banco de dados
                    conn = sqlite3.connect('banco.db')
                    cursor = conn.cursor()

                    # Deletar todas as linhas da tabela
                    cursor.execute('DELETE FROM mensagemEspera')

                    # Salvar as mudanças
                    conn.commit()

                    # Fechar a conexão
                    conn.close()